spring:
  application:
    name: money-flow

  datasource:
    url: jdbc:mariadb://localhost:3306/money_flow
    username: root
    password: 1q2w3e4r

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true