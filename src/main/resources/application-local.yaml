spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/money_flow
    username: root
    password: 1q2w3e4r
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

    show-sql: true